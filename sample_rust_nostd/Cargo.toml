[package]
name = "sample_rust_nostd"
version = "0.1.0"
edition = "2021"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
embedded-hal = "1.0.0"
hashbrown = { version = "0.14.5", default-features = false }
heapless = { version = "0.8.0", default-features = false }
